"use client"
import { useRef } from "react"
import { BookOpen, Brain, Check, Clipboard, Eraser, Lightbulb, Loader2, Menu, RefreshCw, Sparkles, WandSparkles } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Textarea } from "@/components/ui/textarea"
import { useStudyMate, type Utility } from "@/hooks/use-study-mate"
import type { QuizResponse, SummarizeResponse, ExplainResponse, ImproveResponse } from "@/lib/types/api"

const utilities: { id: Utility; title: string; description: string; icon: typeof BookOpen }[] = [
  { id: "summarize", title: "Summarize Notes", description: "Turn long notes into focused revision material.", icon: BookOpen },
  { id: "quiz", title: "Generate Quiz", description: "Test your understanding with multiple-choice questions.", icon: Brain },
  { id: "explain", title: "Explain Concept", description: "Make difficult topics easier to understand.", icon: Lightbulb },
  { id: "improve", title: "Improve Answer", description: "Build clearer, stronger answers for assessments.", icon: WandSparkles },
]
const titleFor = (id: Utility) => utilities.find((item) => item.id === id)?.title
function List({ items }: { items?: string[] }) { return <ul className="space-y-2 text-sm text-muted-foreground">{items?.map((item, index) => <li key={index} className="flex gap-2"><span className="mt-2 size-1.5 shrink-0 rounded-full bg-primary" />{item}</li>)}</ul> }
function Result({ result, utility, onCopy }: { result: unknown; utility: Utility; onCopy: () => void }) {
  if (!result) return <div className="flex min-h-[360px] flex-col items-center justify-center rounded-xl border border-dashed border-border bg-muted/20 p-8 text-center"><Sparkles className="mb-4 size-9 text-primary" /><h3 className="font-semibold">Your AI-generated study result will appear here.</h3><p className="mt-2 max-w-sm text-sm text-muted-foreground">Choose a utility, add your material, and let StudyMate help you learn with more focus.</p></div>
  const data = result as SummarizeResponse & QuizResponse & ExplainResponse & ImproveResponse
  return <div className="space-y-4" aria-live="polite"><div className="flex items-center justify-between"><p className="text-sm font-medium text-muted-foreground">{titleFor(utility)} result</p><Button variant="outline" size="sm" onClick={onCopy}><Clipboard className="mr-2 size-4" />Copy</Button></div>{utility === "summarize" && <><Section title="Summary"><p>{data.summary}</p></Section><Section title="Key Points"><List items={data.key_points} /></Section><Section title="Important Terms"><div className="flex flex-wrap gap-2">{data.important_terms?.map((item) => <span key={item} className="rounded-full bg-primary/10 px-3 py-1 text-xs font-medium text-primary">{item}</span>)}</div></Section><Section title="Revision Tips"><List items={data.revision_tips} /></Section></>}{utility === "quiz" && data.questions?.map((question, index) => <Section key={index} title={`Question ${index + 1}`}><p className="font-medium">{question.question}</p><div className="mt-3 grid gap-2 sm:grid-cols-2">{question.options?.map((option) => <div key={option} className={`rounded-lg border p-3 text-sm ${option === question.correct_answer ? "border-primary/40 bg-primary/5 font-medium" : "border-border"}`}>{option}</div>)}</div><p className="mt-3 text-sm"><strong>Correct answer:</strong> {question.correct_answer}</p><p className="mt-1 text-sm text-muted-foreground">{question.explanation}</p></Section>)}{utility === "explain" && <><Section title="Explanation"><p>{data.explanation}</p></Section><Section title="Analogy"><p>{data.analogy}</p></Section><Section title="Example"><p>{data.example}</p></Section><Section title="Key Takeaways"><List items={data.key_takeaways} /></Section><Section title="Common Mistakes"><List items={data.common_mistakes} /></Section></>}{utility === "improve" && <><Section title="Improved Answer"><p className="whitespace-pre-wrap">{data.improved_answer}</p></Section><Section title="Strengths"><List items={data.strengths} /></Section><Section title="Improvements"><List items={data.improvements} /></Section><Section title="Technical Corrections"><List items={data.technical_corrections} /></Section></>}</div>
}
function Section({ title, children }: { title: string; children: React.ReactNode }) { return <section className="rounded-xl border border-border bg-card p-4"><h3 className="mb-3 text-sm font-semibold text-primary">{title}</h3>{children}</section> }
export default function StudyMateWorkspace() { const state = useStudyMate(); const resultRef = useRef<HTMLDivElement>(null); const { input, setInput } = state; const update = (patch: Partial<typeof input>) => setInput((current) => ({ ...current, ...patch })); const copy = () => { const text = resultRef.current?.innerText; if (text) navigator.clipboard?.writeText(text) }
  return <main className="min-h-screen bg-background text-foreground"><header className="sticky top-0 z-20 border-b border-border/70 bg-background/90 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-4 sm:px-6"><a href="#top" className="flex items-center gap-2 font-semibold"><span className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground"><Sparkles className="size-5" /></span>StudyMate AI</a><nav className="hidden items-center gap-6 text-sm text-muted-foreground md:flex"><a href="#workspace" className="hover:text-foreground">Workspace</a><a href="#utilities" className="hover:text-foreground">Utilities</a></nav><Button size="sm" onClick={() => document.getElementById("workspace")?.scrollIntoView({ behavior: "smooth" })}>Start Studying</Button><Menu className="hidden size-5" /></div></header><div id="top" className="mx-auto max-w-6xl px-4 pb-20 pt-14 sm:px-6 sm:pt-20"><section className="max-w-2xl"><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-primary/20 bg-primary/5 px-3 py-1 text-xs font-medium text-primary"><Sparkles className="size-3.5" />Your focused AI study companion</div><h1 className="text-balance text-4xl font-semibold tracking-tight sm:text-6xl">Study Smarter with AI</h1><p className="mt-5 max-w-xl text-pretty text-lg leading-8 text-muted-foreground">Turn your notes, questions, and answers into useful study material with AI-powered tools.</p></section><section id="utilities" className="mt-14"><div className="mb-5"><p className="text-sm font-medium text-primary">Choose a utility</p><h2 className="mt-1 text-2xl font-semibold">What are you studying today?</h2></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{utilities.map(({ id, title, description, icon: Icon }) => <button key={id} onClick={() => state.setUtility(id)} className={`rounded-xl border p-4 text-left transition hover:-translate-y-0.5 hover:shadow-md ${state.utility === id ? "border-primary bg-primary/5 shadow-sm" : "border-border bg-card"}`} aria-pressed={state.utility === id}><Icon className={`mb-6 size-5 ${state.utility === id ? "text-primary" : "text-muted-foreground"}`} /><h3 className="font-semibold">{title}</h3><p className="mt-1 text-sm leading-5 text-muted-foreground">{description}</p></button>)}</div></section><section id="workspace" className="mt-8 grid scroll-mt-24 gap-5 lg:grid-cols-[minmax(0,0.9fr)_minmax(0,1.1fr)]"><Card><CardHeader><CardTitle className="flex items-center justify-between"><span>{titleFor(state.utility)}</span><Button variant="ghost" size="sm" onClick={state.reset}><Eraser className="mr-2 size-4" />Clear</Button></CardTitle></CardHeader><CardContent className="space-y-5">{(state.utility === "summarize" || state.utility === "quiz") && <><label htmlFor="content" className="text-sm font-medium">Study material</label><Textarea id="content" value={input.content} onChange={(e) => update({ content: e.target.value })} placeholder="Paste your class notes, lecture material, or study content here..." className="min-h-56 resize-y" /><div className="flex items-center justify-between text-xs text-muted-foreground"><span>{input.content.length} characters</span><span>Paste notes or a textbook excerpt</span></div></>}{state.utility === "explain" && <><label htmlFor="topic" className="text-sm font-medium">Concept or topic</label><Textarea id="topic" value={input.topic} onChange={(e) => update({ topic: e.target.value })} placeholder="What concept would you like explained?" className="min-h-40 resize-y" /><label htmlFor="explain-level" className="text-sm font-medium">Difficulty</label><select id="explain-level" className="h-10 w-full rounded-md border border-input bg-background px-3 text-sm" value={input.explainDifficulty} onChange={(e) => update({ explainDifficulty: e.target.value as typeof input.explainDifficulty })}><option value="beginner">Beginner</option><option value="intermediate">Intermediate</option><option value="advanced">Advanced</option></select></>}{state.utility === "improve" && <><label htmlFor="question" className="text-sm font-medium">Question</label><Textarea id="question" value={input.question} onChange={(e) => update({ question: e.target.value })} placeholder="What question are you answering?" /><label htmlFor="answer" className="text-sm font-medium">Your answer</label><Textarea id="answer" value={input.answer} onChange={(e) => update({ answer: e.target.value })} placeholder="Paste your answer here..." className="min-h-40" /></>}{state.utility === "quiz" && <div className="grid grid-cols-2 gap-3"><label className="text-sm font-medium">Questions<select className="mt-2 h-10 w-full rounded-md border border-input bg-background px-3 text-sm" value={input.questionCount} onChange={(e) => update({ questionCount: Number(e.target.value) })}><option value="5">5</option><option value="10">10</option><option value="15">15</option></select></label><label className="text-sm font-medium">Difficulty<select className="mt-2 h-10 w-full rounded-md border border-input bg-background px-3 text-sm" value={input.difficulty} onChange={(e) => update({ difficulty: e.target.value as typeof input.difficulty })}><option value="easy">Easy</option><option value="medium">Medium</option><option value="hard">Hard</option></select></label></div>}{state.error && <div role="alert" className="rounded-lg border border-destructive/30 bg-destructive/5 p-3 text-sm text-destructive">{state.error}{state.result && <Button variant="link" size="sm" onClick={state.submit}>Retry</Button>}</div>}<Button className="w-full" onClick={state.submit} disabled={state.loading}>{state.loading ? <><Loader2 className="mr-2 size-4 animate-spin" />Generating...</> : <><Sparkles className="mr-2 size-4" />{titleFor(state.utility)}</>}</Button></CardContent></Card><Card ref={resultRef} className="min-h-[480px]"><CardHeader><CardTitle>AI Result</CardTitle></CardHeader><CardContent><Result result={state.result} utility={state.utility} onCopy={copy} /></CardContent></Card></section><footer className="mt-20 border-t border-border pt-6 text-sm text-muted-foreground"><div className="flex flex-col justify-between gap-2 sm:flex-row"><span className="font-medium text-foreground">StudyMate AI</span><span>Built for AI-powered learning.</span></div></footer></div></main>
}
